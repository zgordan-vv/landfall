global:
  scrape_interval: 15s
  scrape_timeout: 10s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: landfall-server
    metrics_path: /metrics
    static_configs:
      - targets:
          - server:8080
