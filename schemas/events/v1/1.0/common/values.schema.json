{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.landfall.dev/events/v1/1.0/common/values.schema.json",
  "title": "Landfall bounded protocol values",
  "$defs": {
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]{0,8})\\.(0|[1-9][0-9]{0,8})$",
      "maxLength": 19
    },
    "rfc3339_utc": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]{1,9})?Z$",
      "maxLength": 30
    },
    "uint64_decimal": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]{0,19})$",
      "maxLength": 20,
      "description": "Canonical base-10 unsigned integer in the inclusive range 0..18446744073709551615. JSON Schema enforces the lexical form and digit bound; typed semantic parsing enforces the exact upper bound."
    },
    "int64_decimal": {
      "type": "string",
      "pattern": "^(0|-?[1-9][0-9]{0,18})$",
      "maxLength": 20,
      "description": "Canonical base-10 signed integer in the inclusive range -9223372036854775808..9223372036854775807. JSON Schema enforces the lexical form and digit bound; typed semantic parsing enforces the exact bounds."
    },
    "duration_ns_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Non-negative duration or monotonic-clock reading in nanoseconds."
    },
    "slot_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Solana slot as an exact unsigned decimal string."
    },
    "block_height_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Solana block height as an exact unsigned decimal string."
    },
    "lamports_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Lamports as an exact unsigned decimal string; never a floating-point SOL value."
    },
    "compute_units_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Compute units as an exact unsigned decimal string."
    },
    "confirmation_count_decimal": {
      "$ref": "#/$defs/uint64_decimal",
      "description": "Non-negative confirmation count as an exact unsigned decimal string."
    },
    "token": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
      "maxLength": 64
    },
    "label": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    },
    "bounded_text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 512
    },
    "solana_signature": {
      "type": "string",
      "pattern": "^[1-9A-HJ-NP-Za-km-z]{87,88}$"
    },
    "solana_blockhash": {
      "type": "string",
      "pattern": "^[1-9A-HJ-NP-Za-km-z]{32,44}$"
    },
    "json_pointer": {
      "type": "string",
      "pattern": "^(?:/(?:[^~/]|~[01])*)*$",
      "maxLength": 256
    }
  }
}
